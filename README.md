# WriteUp Monorepo

start all microservices at once with [Docker](https://www.docker.com/products/docker-desktop/)

``` shell
docker compose up --build
```
or run single microservice with node
``` shell
cd "path/to/microservice/root
node .
```
