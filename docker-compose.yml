services:
  api-gateway:
    build: ./api-gateway
    container_name: api-gateway
    ports:
      - "3000:3000"
  books-service:
    build: ./books-service
    container_name: books-service
    ports:
      - "3001:3001"
