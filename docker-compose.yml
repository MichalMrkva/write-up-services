services:
  api-gateway:
    build: ./api-gateway
    container_name: api-gateway
    depends_on:
      - books-service
  books-service:
    build: ./books-service
    container_name: books-service
    ports:
      - 3001:3001
    depends_on:
      - write-up-database-server

volumes:
  db_data:
